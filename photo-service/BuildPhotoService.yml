---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: nitram509/debian-openjdk8-maven-cmake-gcc
    tag: latest

inputs:
- name: PhotoServiceRepo1

outputs:
- name: jar-file


run:
  path: mvn
  args: ["-f", "product-catalog/photo-service/pom.xml", "-DskipTests=true", "package"]
